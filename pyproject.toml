[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bizpulse"
version = "0.1.0"
description = "A business commitment and obligation lifecycle tracking agent powered by Caspian SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "caspian-sdk>=0.1.0",
    "apscheduler>=3.10.0",
    "pytest>=8.0.0",
    "python-dotenv>=1.0.0",
    "python-dateutil>=2.8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[tool.setuptools.packages.find]
where = ["src"]
